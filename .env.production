# Public Cloudinary delivery config (not a secret — appears in every image URL).
# Cloud name is required for ImageSlot to render real images instead of placeholders.
VITE_CLOUDINARY_CLOUD_NAME=dq2z2nf6w
